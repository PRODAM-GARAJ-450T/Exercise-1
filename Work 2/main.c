#include <stdio.h>
#include <math.h>
int main ()
{
	double a, b, c, d, x1, x2, x;
	printf ( "BBEDITE TRI CELbIX 4ISLA \n" );
	scanf ( "%f", &a );
	scanf ( "%f", &b );
	scanf ( "%f", &c );
	return 0;
}
